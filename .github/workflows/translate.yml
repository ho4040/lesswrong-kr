name: Daily translate

on:
  schedule:
    # 매일 22:00 UTC (한국시간 07:00, 출근 시간대)
    - cron: '0 22 * * *'
  workflow_dispatch:
    inputs:
      top_n:
        description: '번역할 글 개수'
        required: false
        default: '1'
      min_score:
        description: '최소 baseScore'
        required: false
        default: '60'
      days:
        description: '최근 며칠'
        required: false
        default: '4'

permissions:
  contents: write

jobs:
  translate:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Seoul
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      TOP_N: ${{ github.event.inputs.top_n || '1' }}
      MIN_SCORE: ${{ github.event.inputs.min_score || '60' }}
      DAYS: ${{ github.event.inputs.days || '4' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps
        working-directory: scripts
        run: npm install

      - name: Run translation
        working-directory: scripts
        env:
          POSTS_DIR: ${{ github.workspace }}/content/posts
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: news
          SITE_BASE_URL: https://ho4040.github.io/lesswrong-kr
        run: npm run translate

      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [[ -n "$(git status --porcelain content/posts)" ]]; then
            git add content/posts
            git commit -m "feat: daily auto-translate $(date +%Y-%m-%d)"
            git pull --rebase origin main
            git push
          else
            echo "No new translations"
          fi
